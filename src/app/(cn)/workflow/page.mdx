import { Guides } from './Guides'
import { Resources } from './Resources'
import { HeroPattern } from '@/components/HeroPattern'
import { WorkflowSite } from '@/config.ts'

export const metadata = {
  title: 'Protium',
  description:
    'Learn everything there is to know about the Protium and integrate tools into your product.',
  image: '/images/python.svg',
}

export const sections = [
  { title: 'Guides', id: 'guides' },
  { title: 'Resources', id: 'resources' },
]

<HeroPattern />

# Protium

Protium Workflow 旨在解决科学计算研究流程中繁琐的、重复的工作。通过将各类计算任务拆分为节点(Node)，实现各任务的模块化集成，高自由复用，并实现自动化运行。

Protium Workflow 页面是用户友好的，仅凭直觉即可快速上手构建你的自动化工作流。我们也制作了[快速上手教程]("#")，来帮助你更快速的了解它的功能。

如果你想深入了解 Workflow 背后发生了什么，不妨阅读我们的[设计理念](“#”)，这将帮助你了解其背后发生的过程，或者，你也可以阅读[开发者手册]("#")来自定义一个你的自定义节点，实现更符合你的需求的自定义工作流。

最后，当你实现一个工作流后，你可以将其发布到我们的「[数流](#)社区（**Flo**w so**ciety**）」中，又或者其中寻找你需要的工作流。

<div className="not-prose mb-16 mt-6 flex gap-3">
  <Button href="/workflow/quickstart" arrow="right">
    <>快速开始</>
  </Button>
  <Button href={`${WorkflowSite}/flociety/nodes`} variant="outline">
    <>探索「数流」社区</>
  </Button>
</div>

## Getting started {{ anchor: false }}

要立即开始，在你的 <a href={WorkflowSite}>Workspace</a> 中创建一个工作流，然后跟随我们的 [Quickstart](/workflow/quickstart) 来创建一个你的样例。
当流程测试完成后，你可以发布到我们的数流社区，让整个社区的开发者都能使用您的成果。{{ className: 'lead' }}

运行工作流需要消耗计算资源, 所以，请设置你的计算资源的访问令牌 Access Token. {{ className: 'lead' }}

<div className="not-prose">
  <Button href="#" variant="text" arrow="right">
    <>设置你的访问令牌</>
  </Button>
</div>


<Guides />  

<Resources /> 
