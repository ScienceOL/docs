# 部署

要部署 Xyzen，首先你需要部署一个中间件，中间件使用 Docker 启动：

```bash
docker compose -f ./docker/docker-compose.mid.yaml --env-file ".env.example"
```

启动中间件后，运行如下命令启动 xyzen 后端服务。我们建议你使用 uv 进行项目管理。

```sh
# 进入后端项目
cd service 
# 安装依赖
uv sync -dev
# 激活环境
source ./.venv/bin/activate
# 启动服务
python -m cmd.main
```